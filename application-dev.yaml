logging:
  level:
    root: INFO
    org.springframework: INFO
    org.springframework.web: INFO

management:
  tracing:
    sampling:
      probability: 1.0 
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
