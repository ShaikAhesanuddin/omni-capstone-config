spring:
  datasource:
    url: jdbc:mysql://mysql-inventory:3306/inventory_db
    username: root
    password: ${DATABASE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
