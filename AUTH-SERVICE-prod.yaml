spring:
  config:
    import: optional:aws-secretsmanager:bazario/auth-service
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

logging:
  level:
    io.github.resilience4j: WARN
