spring:
  datasource:
    url: jdbc:mysql://mysql-auth:3306/auth_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    io.github.resilience4j: WARN
