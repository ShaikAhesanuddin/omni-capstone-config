spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: "@Ahesan1810"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    io.github.resilience4j: DEBUG
