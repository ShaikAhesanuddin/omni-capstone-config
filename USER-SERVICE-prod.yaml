spring:
  datasource:
    url: jdbc:mysql://mysql-users:3306/user_db
    username: root
    password: ${DATABASE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    root: WARN
